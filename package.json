{
  "name": "pinthip-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx http-server . -p 8000 -c-1",
    "lint": "eslint . --ext .js",
    "test": "node --test tests/**/*.test.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
